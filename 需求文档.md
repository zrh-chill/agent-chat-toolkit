项目 1：AI 聊天助手 + 工具调用
项目名称

agent-chat-toolkit

项目目标

做一个最小但完整的 Agent 聊天后端。它不是普通聊天机器人，而是可以根据用户问题自动选择工具，例如计算、查天气 mock、查数据库、生成待办事项。

这个项目的目标是让你熟悉：

LLM API 调用
tool/function calling
结构化输出
FastAPI 接口
streaming 响应
基础错误处理
Agent 项目目录组织
推荐技术栈
Python
FastAPI
Pydantic
OpenAI Agents SDK 或 Pydantic AI
SQLite
pytest
optional：React/Next.js 简单前端
核心功能
1. 基础聊天接口

提供接口：

POST /chat

用户输入：

{
  "message": "帮我计算 235 * 18，然后生成一个待办事项"
}

系统返回：

{
  "answer": "...",
  "tool_calls": [
    {
      "tool": "calculator",
      "input": "...",
      "output": "4230"
    }
  ]
}
2. 工具调用

至少实现 4 个工具：

工具	功能
calculator	做简单计算
todo_create	创建待办事项
todo_list	查询待办事项
fake_weather	返回 mock 天气信息
3. 结构化输出

让 Agent 输出固定结构，例如：

class AgentResponse(BaseModel):
    answer: str
    intent: str
    tool_used: list[str]
    confidence: float
4. 对话历史

把用户消息、Agent 回复、tool 调用记录保存到 SQLite。

5. 流式输出

增加：

POST /chat/stream

让前端能逐字显示回复。

验收标准

你完成后应该能做到：

README 写清楚启动方式
有 .env.example
有 3–5 个测试用例
至少 4 个工具
tool 调用过程可记录
报错时不会直接崩溃
支持普通返回和 streaming 返回
加分项
做一个简单网页聊天界面
工具调用过程在前端可视化
每次请求显示 token 成本或耗时